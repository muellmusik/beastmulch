{\rtf1\mac\ansicpg10000\cocoartf824\cocoasubrtf440
{\fonttbl\f0\fnil\fcharset77 Monaco;}
{\colortbl;\red255\green255\blue255;\red0\green0\blue191;\red96\green96\blue96;\red191\green0\blue0;
\red13\green38\blue178;\red191\green0\blue0;\red0\green0\blue0;}
\deftab560
\pard\pardeftab560\ql\qnatural

\f0\fs18 \cf0 (\
\
~outs = \cf2 this\cf0 .executeFile(\cf2 Document\cf0 .current.dir.dirname ++ \cf3 "/january channel list.rtf".standardizePath\cf0 )[0];\
~maxX = ~outs.collect(\{|assoc| assoc.value.x.abs \}).maxItem;\
~yvals = ~outs.collect(\{|assoc| assoc.value.y \});\
~hiY = ~yvals.maxItem / ~maxX;\
~lowY = ~yvals.minItem / ~maxX;\
~ySIze = ~hiY - ~lowY;\
w = \cf2 SCWindow\cf0 (\cf3 "Simple QC Test"\cf0 , ~rect = \cf2 Rect\cf0 (100,200, 1000 / ([~hiY, ~lowY].abs.maxItem) * 0.8, 1000)).front;\
\cf4 //SCButton(w, Rect(0, 0, 150, 20))\cf0 \
\cf4 //	.states_([["pick another QC file"]])\cf0 \
\cf4 //	.action_(\{ File.openDialog("", \{ |path| m.path_(path) \}) \});\cf0 \
m = \cf2 SCQuartzComposerView\cf0 (w, ~rect.moveTo(0,0));\
m.path = \cf2 Document\cf0 .current.path.dirname ++ \cf3 "/SwarmLayout3.qtz"\cf0 ;\
\
//~colours = Pseq\
m.startY = ~hiY;\
m.endY = ~lowY;\
~floorZ = ~outs.collect(\{|assoc| assoc.value.z \}).minItem / ~maxX;\
m.floorZ = ~floorZ;\
r = ~outs.collect(\{|assoc| \
	var x, y, z;\
	x = assoc.value.x / ~maxX;\
	y = assoc.value.y / ~maxX;\
	z = assoc.value.z / ~maxX;\
	\
	[x, y, z, \cf2 Color\cf0 .green.alpha_(0.6), Point(x, y).theta * 57.295779513082, assoc.key.asString] \
	\
\});\
\
k = KDTree(~outs.collect(\{|assoc| \
	var x, y, z;\
	x = assoc.value.x / ~maxX;\
	y = assoc.value.y / ~maxX;\
	z = assoc.value.z / ~maxX;\
	\
	[x, y, z, assoc.value.index] \
	\
\}), lastIsLabel: \cf5 true\cf0 );\
\
//k.nearest([0, 0, 0])[0].label\
\
\cf6 //\cf7 \
\cf6 //\cf7 \
\cf6 //p = Polar(0.5, 0);\cf7 \
\cf6 //r = \{ p = p.rotate(0.25pi); [p.asPoint.x, p.asPoint.y, 0.1, Color.new255(139, 123, 139).alpha_(0.6), p.theta * 57.295779513082, "8030"]\} ! 8;\cf7 \
\cf6 //\cf7 \
\cf6 //p = p.rotate(0.125pi);\cf7 \
\cf6 //\cf7 \
\cf6 //p = p.scale(0.8);\cf7 \
\cf6 //r = r ++ (\{ p = p.rotate(0.25pi); [p.asPoint.x, p.asPoint.y, 0.2, Color.green.alpha_(0.6), p.theta * 57.295779513082, "8040"]\} ! 8);\cf7 \
\cf6 //\cf7 \
\cf6 //p = p.scale(2);\cf7 \
\cf6 //\cf7 \
\cf6 //r = r ++ (\{ p = p.rotate(0.25pi); [p.asPoint.x, p.asPoint.y, 0.15, Color.red.alpha_(0.6), p.theta * 57.295779513082, "ATC"]\} ! 8);\cf7 \
\cf0 \
//m.showSpeakers = \cf2 false\cf0 ;\
m.speakers = r;\
m.start;\
\cf4 // dim = 3, numBoids, centre, limits, velMax, velScale, minDist\cf0 \
b = \cf2 BMGrainBoidSpace\cf0 (3, 20, \cf2 nil\cf0 , [[-0.9, -0.9, 0.1], [0.9, 0.9, 0.2]], 0.2, 1, 0.07);\
\
\cf2 AppClock\cf0 .sched(0, \{\
b.numBoids.do(\{b.moveNext\});\
m.positions = b.boids.collect(\cf2 _\cf0 .pos);\
0.05\
\});\
)\
\
m.showSpeakers = \cf2 true\cf0 \
m.showSpeakers = \cf2 false\cf0 \
\
b.centre = [0.5, 0.5, 0]\
\
b.centre = [0, 0, 0.2]\
\
b.centre = \cf2 nil\cf0 ;\
\
b.boids.collect(\cf2 _\cf0 .pos);\
\
b.minDist = 0.05\
  \
b.velMax = 0.07\
\
b.avoidD = \cf2 true\cf0 ;\
\
b.velScale = 0.1\
\
b.velScale = 0.9}